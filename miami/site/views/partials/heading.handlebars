{{!-- this if is a helpre to evulate if the variable expresion exists --}}
{{#if data.image}}
<img class="hero img-fluid" src="{{data.image}}" alt="">
{{/if}}

<h1 class="primary-heading">{{data.title}}</h1>
<p class="abstract">{{data.abstract}}</p>